# CrewRoster Environment Configuration - Firebase
# Copy this file to backend/.env and customize the values

# JWT Secrets (generate with: openssl rand -hex 64)
JWT_SECRET=change-this-to-a-random-secret-at-least-64-chars
JWT_REFRESH_SECRET=change-this-to-another-random-secret

# Registration (set a secret to allow user registration)
REGISTRATION_SECRET=change-this-to-a-secret-for-registration

# Server port (for standalone mode, e.g. Render)
PORT=4000

# Frontend URL for CORS
FRONTEND_URL=https://crewroster-app.web.app

# For local development with Firebase Emulators:
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# For production:
# GOOGLE_APPLICATION_CREDENTIALS=path/to/serviceAccountKey.json
