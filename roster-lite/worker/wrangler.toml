name = "crewroster-proxy"
main = "worker.js"
compatibility_date = "2024-09-01"

# Configuração (CREWLINK_BASE / ALLOWED_ORIGINS) está hardcoded em worker.js —
# o ficheiro é autónomo para poder ser colado no dashboard sem variáveis.
